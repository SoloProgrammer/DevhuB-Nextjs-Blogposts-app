"use client"
import ErrorPage from "@/components/Error/Error";
import React from "react";

const Error = (props) => {
  return <ErrorPage {...props} key={"getUserError"} />;
};

export default Error;
